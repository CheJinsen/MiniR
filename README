MiniR

This program is the Minimial/subset of R(The R Project for Statistical Computing) or GSL(GNU Scientific Library). It just include random distribution functions(density/probability/quantile/random) currently. It passed test on Ubuntu-20.04/FreeBSD-12.1.

build:
	cd ../build
	rm CMakeCache.txt 	//if exist, first compile
	cmake ..

run:
	../bin/main

